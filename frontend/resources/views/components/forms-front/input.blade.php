<div class="row mb-3">
    <label class="col-sm-2 col-form-label" for="{{ $neme }}">{{ $label }}</label>
    <div class="col-sm-10">
        <input type="text" name="{{ $name }}" id="{{ $name }}" class="form-control" value="{{ $value }}"
            disabled placeholder="{{ $placeholder }}">
    </div>
</div>
